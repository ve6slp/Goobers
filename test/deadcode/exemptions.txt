# Exact analyzer identities only. Every entry requires a concrete reason;
# test/deadcode rejects broad matching, duplicate entries, and stale exemptions.

# Public API and compatibility surfaces.
github.com/goobers/goobers/internal/workflow/v_next.dslVersionLevel # Per-version level lookup reachable only through the release-gate evolution validation (#3307 wave), which the tagged-baseline tests drive; the admission path never calls it.
github.com/goobers/goobers/api/v1alpha1.WriteArtifact # Documented stage-contract helper retained for external stage implementations.
github.com/goobers/goobers/api/v1alpha1.Resource # Conventional Kubernetes API helper retained for external CRD consumers.
github.com/goobers/goobers/api/v1alpha1.RegisteredKinds # Public CRD inventory retained for config and tooling consumers.
github.com/goobers/goobers/internal/configdiff.OperationalFields # Documented operational-tuning allowlist, exercised directly by TestOperationalFields (#2044 audit: was vaguely "audit and tooling consumers" with no named consumer).
github.com/goobers/goobers/internal/credentials.NewResolverWithSources # Dynamic-source resolver constructor (#686) retained as public API, exercised by credential resolver tests.
github.com/goobers/goobers/internal/gooberassets.IsWithinSourceDir # Embedded-asset boundary helper, exercised by assets_test.go's hidden-directory case (#2044 audit).
github.com/goobers/goobers/internal/missioncontrol.EvaluateMetric # Versioned mission-control metric contract entry point retained for workflow consumers (#1449).
github.com/goobers/goobers/internal/missioncontrol.Build # Versioned mission-control artifact builder retained for workflow consumers (#1449).
github.com/goobers/goobers/internal/missioncontrol.aggregate # Mission-control aggregation helper reachable through the retained artifact builder (#1449).
github.com/goobers/goobers/internal/missioncontrol.compare # Mission-control criterion evaluator reachable through retained contract entry points (#1449).
github.com/goobers/goobers/internal/missioncontrol.Artifact.Validate # Versioned mission-control artifact validator retained for workflow consumers (#1449).
github.com/goobers/goobers/internal/missioncontrol.validateArtifactMetadata # Mission-control artifact metadata validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateEvidence # Mission-control evidence validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateSubsystemDefinitions # Mission-control subsystem-definition validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateMetrics # Mission-control metrics-section validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateSubsystems # Mission-control subsystem-section validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateOverall # Mission-control overall-section validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateMetric # Mission-control metric validator reachable through the retained artifact validator (#1449).
github.com/goobers/goobers/internal/missioncontrol.validateMetricMetadata # Mission-control metric metadata validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateMetricCriterion # Mission-control metric criterion validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateMetricValue # Mission-control metric value validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateMetricTiming # Mission-control metric timing validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateMetricStatus # Mission-control metric status validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validMetricReason # Mission-control metric verdict-reason guard reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateMetricObservation # Mission-control metric observation validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateConclusiveMetricObservation # Mission-control conclusive-observation validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateStaleMetricObservation # Mission-control stale-observation validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateMetricWithoutCanonicalValue # Mission-control canonical-value validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateMissingMetricObservation # Mission-control missing-observation validator reachable through the retained artifact validator (#3038).
github.com/goobers/goobers/internal/missioncontrol.validateAggregation # Mission-control aggregate validator reachable through the retained artifact validator (#1449).
github.com/goobers/goobers/internal/missioncontrol.validateMembers # Mission-control membership validator reachable through the retained artifact validator (#1449).
github.com/goobers/goobers/internal/missioncontrol.uniqueID # Mission-control identifier validator reachable through the retained artifact validator (#1449).
github.com/goobers/goobers/internal/missioncontrol.criterionFinite # Mission-control finite-criterion guard reachable through retained contract entry points (#1449).
github.com/goobers/goobers/internal/missioncontrol.validCriterionShape # Mission-control criterion-shape guard reachable through the retained artifact validator (#1449).
github.com/goobers/goobers/internal/missioncontrol.finite # Mission-control finite-number guard reachable through retained contract entry points (#1449).
github.com/goobers/goobers/internal/missioncontrol.validVerdict # Mission-control verdict enum guard reachable through the retained artifact validator (#1449).
github.com/goobers/goobers/internal/missioncontrol.validReasonCode # Mission-control reason-code enum guard reachable through the retained artifact validator (#1449).
github.com/goobers/goobers/internal/missioncontrol.canonicalize # Mission-control deterministic ordering helper reachable through the retained artifact builder (#1449).
github.com/goobers/goobers/internal/missioncontrol.RenderFacts # Exact mission-control facts renderer retained for workflow consumers (#1449).
github.com/goobers/goobers/internal/missioncontrol.renderCriterion # Mission-control criterion renderer reachable through the retained exact-facts renderer (#1449).
github.com/goobers/goobers/internal/telemetry/rollup.Rebuild # Single-root convenience wrapper over RebuildAll, retained for the CLI trace/stats tests; its last non-test caller was the scale harness, which moved to multi-root RebuildAll when runs became per-gaggle (#1913).
github.com/goobers/goobers/internal/readmodel.SupportedCombinations # Exposes the closed set for contract generation and the API-surface enumeration test (#1920).
github.com/goobers/goobers/internal/httpapi.SurfaceActions # API capability inventory, exercised by cmd/goobers/runtime_capabilities_test.go's cross-surface parity check and mutations_test.go's runtime-mutation classification (#2044 audit).
github.com/goobers/goobers/internal/builtincmd.Names # Built-in subcommand inventory accessor, exercised by cmd/goobers/builtincmd_parity_test.go's CLI-registry parity check (#2861 admission hardening).
github.com/goobers/goobers/internal/telemetry.AllAttributes # Telemetry schema inventory, exercised by telemetry_test.go and otlp_test.go's attribute-coverage checks (#2044 audit).
github.com/goobers/goobers/internal/telemetry.KnownAttribute # Telemetry schema validator, exercised by telemetry_test.go's legacy-attribute-rejection case (#2044 audit).
github.com/goobers/goobers/internal/webhook.ValidSignature # Webhook signature helper, exercised by webhook_test.go's valid/invalid-signature cases (#2044 audit).
github.com/goobers/goobers/internal/workflow.FeatureRegistry.Lookup # Version-routed registry API exercised by coexistence tests and retained with FeatureRegistry.All for immutable lookup parity (#2981).
github.com/goobers/goobers/internal/workflow.FeatureRegistry.All # Version-routed registry API exercised by coexistence tests and retained with FeatureRegistry.Lookup for immutable lookup parity (#2981).
github.com/goobers/goobers/internal/workflow.LookupFeature # Workflow feature-lookup facade, exercised by coexistence_test.go and test/workflowupgrade/advisor_test.go (#2044 audit).
github.com/goobers/goobers/internal/workflow.CheckStageContractWarnings # Stage-contract advisory facade, exercised by test/shippedworkflows/contracts_test.go (#2044 audit).
github.com/goobers/goobers/internal/workflow/v_next.CheckGaggleFeatureSupport # Next-interpreter Gaggle policy API retained for versioned-interpreter parity and exercised by preview-policy tests (#2046).
github.com/goobers/goobers/internal/workflow/v_next.LookupFeature # Next-interpreter feature lookup retained as part of the independently versioned interpreter API.
github.com/goobers/goobers/providers.WithADOMaxRateLimitRetries # Public ADO retry option retained for external provider consumers.
github.com/goobers/goobers/providers.WithRateLimitMaxWait # Public provider tuning option retained for external callers with stage-specific time budgets.
github.com/goobers/goobers/providers.WithGiteaSecretRegistrar # Public Gitea secret-registrar option retained for external provider consumers; mirrors WithADOSecretRegistrar.
github.com/goobers/goobers/providers.WithGiteaHTTPClient # Public Gitea HTTP-client injection option retained for external provider consumers and custom transports.
github.com/goobers/goobers/providers.WithGiteaMaxRateLimitRetries # Public Gitea retry option retained for external provider consumers; mirrors WithADOMaxRateLimitRetries.
github.com/goobers/goobers/providers.WithGiteaMaxTransientRetries # Public Gitea transient-retry option retained for external provider consumers.
github.com/goobers/goobers/providers.WithGiteaRateLimitMaxWait # Public Gitea rate-limit wait-budget option retained for external provider consumers; mirrors WithRateLimitMaxWait.
github.com/goobers/goobers/providers.BranchName # Public provider branch helper retained for external provider consumers.
github.com/goobers/goobers/telemetryconnector/v1alpha1.RegisterFactory # Public connector-extension registration API retained for externally linked adapters.
github.com/goobers/goobers/telemetryconnector/v1alpha1.MustRegisterFactory # Init-time connector-extension registration API retained for externally linked adapters.
github.com/goobers/goobers/telemetryconnector/v1alpha1.NewQueryError # Public connector failure-classification helper retained for external adapters.

# Cross-surface contract invariants exercised by drift tests.
github.com/goobers/goobers/providers.BlessedTierProviderKinds # Blessed-tier CI gate's provider set, invoked by TestBlessedTierGapsAreTracked (CONF-2, #2075).
github.com/goobers/goobers/providers.ValidateBlessedTier # Blessed-tier CI gate invoked by TestBlessedTierGapsAreTracked (CONF-2, #2075).
github.com/goobers/goobers/internal/apicontract.ValidateRuntimeParity # Contract generator invariant retained for cross-surface drift tests.
github.com/goobers/goobers/internal/apicontract.validSurface # Contract generator helper reachable through the drift-test invariant.
github.com/goobers/goobers/internal/apicontract.V1ConfigAuthoringRoutes # Config-authoring route contract (#3779) not yet registered by the daemon until its backing services land; exercised by authoring_test.go.

# Test and fixture support intentionally shared across package boundaries.
github.com/goobers/goobers/internal/flake.Quarantine # Issue-backed quarantine API (#664) intentionally ships with zero occupants until a flake is diagnosed.
github.com/goobers/goobers/internal/flake.quarantine # Clock-injected quarantine helper exercised by expiry and validation tests.
github.com/goobers/goobers/internal/harness.ConformanceCoveredAdapterNames # Cross-package registry-completeness contract (#2776), exercised by cmd/goobers's TestBuildHarnessRegistryAdaptersAreConformanceCovered.
github.com/goobers/goobers/internal/providerstage.Commands # Cross-package provider-stage inventory exercised by cmd/goobers's blessed-tier dispatch conformance gate (CONF-10, #2499).
github.com/goobers/goobers/telemetryconnector/v1alpha1/contracttest.Run # Shared connector conformance helper retained for built-in and external adapter tests.

# Test seams retained to exercise fail-closed and lifecycle behavior.
github.com/goobers/goobers/internal/supportmatrix.validateSupportMatrixEvolution # Support-matrix evolution guard (DVL-7) exercised by release-history tests.
github.com/goobers/goobers/internal/workflow/v_next.newFeatureRegistryAgainstReleased # Next-interpreter feature-registry evolution guard (VER-3) exercised by workflow tests, retained pending a released baseline to enforce against.
github.com/goobers/goobers/internal/workflow/v_next.validateFeatureRegistryEvolution # Next-interpreter feature-registry evolution validator (VER-3) exercised by workflow tests, retained pending a released baseline to enforce against.
github.com/goobers/goobers/internal/workflow/v_3_0.dslVersionLevel # 3.0-interpreter copy of the v_next per-version level lookup, reachable only through the release-gate evolution validation the tagged-baseline tests drive (#3505).
github.com/goobers/goobers/internal/workflow/v_3_0.CheckGaggleFeatureSupport # 3.0-interpreter Gaggle policy API retained for versioned-interpreter parity and exercised by preview-policy tests (#3505, mirrors the v_next entry).
github.com/goobers/goobers/internal/workflow/v_3_0.LookupFeature # 3.0-interpreter feature lookup retained as part of the independently versioned interpreter API (#3505).
github.com/goobers/goobers/internal/workflow/v_3_0.newFeatureRegistryAgainstReleased # 3.0-interpreter feature-registry evolution guard exercised by the release-baseline tests (#3505, mirrors the v_next entry).
github.com/goobers/goobers/internal/workflow/v_3_0.validateFeatureRegistryEvolution # 3.0-interpreter feature-registry evolution validator exercised by the release-baseline tests (#3505, mirrors the v_next entry).
github.com/goobers/goobers/cmd/goobers.promptGuidedOptions # Guided target-input prompt seam retained for focused parser and credential-reference tests.
github.com/goobers/goobers/cmd/goobers.reconcileMergeReviewStatusComment # Merge-review reconciliation seam retained for command tests.
github.com/goobers/goobers/cmd/goobers.refreshBlockedEligibility # Blocked-record refresh seam retained for command tests.
github.com/goobers/goobers/cmd/goobers.resumeInterruptedRuns # Crash-recovery seam retained for daemon tests.
github.com/goobers/goobers/cmd/goobers.waitForRunTerminal # Run-wait seam retained for command tests with controlled contexts.
github.com/goobers/goobers/cmd/goobers.listRuns # Run-list seam retained for command tests with controlled output.
github.com/goobers/goobers/cmd/goobers.cliSurfaceActions # Runtime-capability registry seam retained for parity tests.
github.com/goobers/goobers/cmd/goobers.cliSurfaceActionsFrom # Runtime-capability registry seam retained for parity tests.
github.com/goobers/goobers/cmd/goobers.runTraceWithFollowContext # Trace follow seam retained for cancellation tests.
github.com/goobers/goobers/cmd/goobers.runTraceWithFollowContextAndFactory # Trace follow seam retained for cancellation tests.
github.com/goobers/goobers/internal/harness.WithTranscriptLimit # Harness transcript-boundary option retained for executor tests.
github.com/goobers/goobers/internal/localscheduler.WithClock # Scheduler clock option retained for deterministic trigger tests.
github.com/goobers/goobers/internal/platform/shutdown.DrainWithEscalation # Shutdown drain helper retained for bounded lifecycle tests.
github.com/goobers/goobers/internal/worktree.WithPinnedProcessKiller # Pinned-workspace process-termination injection seam retained for the lock-aware reset lifecycle test.
github.com/goobers/goobers/internal/worktree.setObjectCacheLockTimeoutForTest # Object-cache lock-timeout test seam (#2905), used only from objectcachelock_test.go — mirrors internal/journal.SetLockTimeoutForTest.

# Quarantined, reserved, and extension seams required by the architecture.
# Notification output boundary (#1451) ships ahead of its mission-control
# workflow caller (#1450); each entry is exercised by notification package tests.
github.com/goobers/goobers/internal/notification.NewJournalRecorder # Durable notification journal adapter reserved for the mission-control workflow caller (#1450).
github.com/goobers/goobers/internal/notification.JournalRecorder.RecordRequest # Notification request persistence method reached through the reserved recorder boundary (#1450).
github.com/goobers/goobers/internal/notification.JournalRecorder.RecordReceipt # Notification receipt persistence method reached through the reserved recorder boundary (#1450).
github.com/goobers/goobers/internal/notification.JournalRecorder.recordReceipt # Locked receipt append helper reached through the reserved recorder boundary (#1450).
github.com/goobers/goobers/internal/notification.JournalRecorder.ClaimDelivery # Atomic delivery-claim method reached through the reserved recorder boundary (#1450).
github.com/goobers/goobers/internal/notification.Policy.normalized # Notification policy validator reached through the reserved dispatcher constructor (#1450).
github.com/goobers/goobers/internal/notification.NewDispatcher # Notification dispatcher constructor reserved for the mission-control workflow caller (#1450).
github.com/goobers/goobers/internal/notification.Dispatcher.Dispatch # Notification dispatch entrypoint reserved for the mission-control workflow caller (#1450).
github.com/goobers/goobers/internal/notification.Dispatcher.dispatchSink # Per-sink delivery helper reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.Dispatcher.persist # Durable receipt helper reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.startDelivery # Bounded asynchronous sink helper reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.deliveryAttempt.wait # Delivery timeout helper reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.Dispatcher.resolvePending # Timed-out delivery resolution helper reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.Dispatcher.receipt # Delivery receipt builder reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.idempotencyDigest # Receipt idempotency helper reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.sinkRef # Provider-neutral sink reference helper reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.ValidateRequest # Notification contract validator reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.validSHA256Digest # Evidence digest validator reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.Dispatcher.sanitizeError # Credential-aware error sanitizer reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.NewRegistry # Notification sink registry constructor reserved for the mission-control workflow caller (#1450).
github.com/goobers/goobers/internal/notification.Registry.Register # Notification sink registration entrypoint reserved for the mission-control workflow caller (#1450).
github.com/goobers/goobers/internal/notification.Registry.lookup # Sink lookup helper reached through the reserved dispatch entrypoint (#1450).
github.com/goobers/goobers/internal/notification.RecordingSink.Kind # Hermetic sink contract method retained for shared notification tests (#1451).
github.com/goobers/goobers/internal/notification.RecordingSink.Version # Hermetic sink contract method retained for shared notification tests (#1451).
github.com/goobers/goobers/internal/notification.RecordingSink.Deliver # Hermetic sink delivery method retained for shared notification tests (#1451).
github.com/goobers/goobers/internal/notification.RecordingSink.Requests # Hermetic sink assertion accessor retained for shared notification tests (#1451).
github.com/goobers/goobers/internal/notification.NewTerminalSink # Development terminal sink constructor reserved for notification-output callers (#1451).
github.com/goobers/goobers/internal/notification.TerminalSink.Kind # Development terminal sink contract method reserved for notification-output callers (#1451).
github.com/goobers/goobers/internal/notification.TerminalSink.Version # Development terminal sink contract method reserved for notification-output callers (#1451).
github.com/goobers/goobers/internal/notification.TerminalSink.Deliver # Development terminal sink delivery method reserved for notification-output callers (#1451).
github.com/goobers/goobers/internal/configsync.NoopApplier.Apply # Quarantined tier-3 direct-apply seam retained per docs/ARCHITECTURE.md section 11.
github.com/goobers/goobers/internal/engine.NewRegistry # Temporal engine registry constructor retained for tier 3, exercised by registry package tests.
github.com/goobers/goobers/internal/engine.ProjectCompletedScheduledRun # Temporal scheduled-run projection entrypoint (#635) retained for tier 3, exercised by schedule integration tests.
github.com/goobers/goobers/internal/engine.ProjectSchedulerEvents # Temporal scheduler-journal projection helper (#635) retained for tier 3, exercised by schedule tests.
github.com/goobers/goobers/internal/engine.NewScheduleReconciler # Temporal schedule reconciler constructor (#635) retained for quarantined tier 3, exercised by worker integration tests.
github.com/goobers/goobers/internal/engine.ScheduleReconciler.Reconcile # Temporal schedule reconciliation entrypoint (#635) retained for quarantined tier 3, exercised by schedule integration tests.
github.com/goobers/goobers/internal/engine.ScheduleReconciler.reconcileDurably # Durable Temporal schedule reconciliation helper (#635) reachable through the quarantined tier-3 entrypoint.
github.com/goobers/goobers/internal/engine.ScheduleClaimID # Deterministic Temporal schedule-fire claim helper (#635) retained for tier-3 diagnostics and integration assertions.
github.com/goobers/goobers/internal/harness.Registry.Register # Harness extension registry retained for additional adapters.
github.com/goobers/goobers/internal/journal.ConformanceBranches # Per-branch conformance projection retained for the tier-3 harness; seq is not comparable across branches.
github.com/goobers/goobers/internal/journal.MonotonicSeq # Cross-runner journal invariant retained for the tier-3 harness.
github.com/goobers/goobers/internal/journal.SetLockTimeoutForTest # Lock-timeout test seam used only from _test.go files: cmd/goobers's live-daemon abort-delegation tests (#2270) and the journal-plane adoption tests that shrink the 30s bound instead of waiting it out (#3828).
github.com/goobers/goobers/internal/localscheduler.ActiveRunCounts # Scheduler readiness helper retained for alternate trigger integrations.
github.com/goobers/goobers/internal/platform/shutdown.NewExternal # Windows service shutdown entrypoint retained for SCM integration.
github.com/goobers/goobers/internal/platform/shutdown.Notifier.Reason # Shutdown reason accessor retained for service and diagnostics integrations.

# Test-support constructors: reachable only from _test.go, so unreachable from any main.
github.com/goobers/goobers/internal/temporaltest.NewWorkflowEnvironment # Shared Temporal test-environment constructor (#1735) used by every engine and e2e workflow test.
github.com/goobers/goobers/internal/temporaltest.ProjectionQuerier.QueryWorkflow # Adapts the test environment to engine's projectionQuerier shape so ProjectCompletedRun* can run hermetically (#2903); used by test/e2e's engine-start/engine-project round trip.
github.com/goobers/goobers/internal/testgit.Command # Isolated git command constructor used across repository fixture tests (#1811).
github.com/goobers/goobers/internal/testgit.AmbientCommand # Explicit ambient git command constructor used by environment-boundary tests (#1811).

# Read-model conformance contract (#1921): a backend-neutral suite imported by
# each backend's _test.go, so it is unreachable from any main by construction.
# It is deleted when the read model is, not before; a second backend (#652)
# will import the same entries.
github.com/goobers/goobers/internal/readmodel/conformance.Run # Contract entrypoint: the whole suite a backend is handed to.
github.com/goobers/goobers/internal/readmodel/conformance.absentRunIsAnAnswerNotAnError # Contract case: retention must not look like breakage.
github.com/goobers/goobers/internal/readmodel/conformance.changeOrderIsSerializedAndDense # Contract case: resuming after a position misses nothing (SS8.2).
github.com/goobers/goobers/internal/readmodel/conformance.changeSeqIsNeverReused # Contract case: pruned positions are not handed out again.
github.com/goobers/goobers/internal/readmodel/conformance.closedSetIsFullyServed # Contract case: every declared combination is actually answerable.
github.com/goobers/goobers/internal/readmodel/conformance.closedSetIsRefusedNotAnswered # Contract case: an unlisted combination is refused, not answered slowly (SS5.7).
github.com/goobers/goobers/internal/readmodel/conformance.completedRun # Contract fixture builder.
github.com/goobers/goobers/internal/readmodel/conformance.epochsAreOpaqueAndDistinct # Contract case: a fresh store never reuses an epoch (SS4.2).
github.com/goobers/goobers/internal/readmodel/conformance.freshnessIsSourceRelative # Contract case: freshness is a source position, never a clock (SS14.7).
github.com/goobers/goobers/internal/readmodel/conformance.guardedAcknowledgement # Contract case: a stale projection must not overwrite a newer one.
github.com/goobers/goobers/internal/readmodel/conformance.optionsForDims # Contract fixture builder: maps a declared dimension set to a request.
github.com/goobers/goobers/internal/readmodel/conformance.pagesAreBoundedAndOrdered # Contract case: page limits and the stable tiebreak keyset cursors need.
github.com/goobers/goobers/internal/readmodel/conformance.replayIsIdempotent # Contract case: same-position replay emits no change (caught a real defect).
github.com/goobers/goobers/internal/readmodel/conformance.retentionFloorOnlyAdvances # Contract case: the floor never moves backwards.
github.com/goobers/goobers/internal/readmodel/conformance.runID # Contract fixture builder.

# Intake store (#1922): the source-watermark database. Its callers land in Wave 3
# — the projector (#1923) drains and acknowledges, repair (#1924) inspects single
# markers, retention (#1932) records removal intent. These entries are expected to
# go STALE and be deleted as each of those lands; a stale entry fails this gate.
github.com/goobers/goobers/internal/readmodel/intake.PathFor # Resolves intake.db inside an instance directory for every opener.

# Repair sweep observability (#1924). Stats feeds the freshness envelope (#1927,
# Wave 4), which reports sweep cycle time and degraded conditions. Expected to go
# stale when that lands.
github.com/goobers/goobers/internal/readmodel/repair.Sweeper.Stats # Sweep counters for the freshness envelope (#1927).

# readState envelope (#1927). ParseSourceApplied decodes the If-Source-Applied
# request header; its caller is the HTTP layer's read-your-write check, which
# lands with the route wiring in the same wave. Expected to go stale then.
github.com/goobers/goobers/internal/readmodel.ParseSourceApplied # If-Source-Applied header decoder (#1927 route wiring).

# Mutability seam (#1934). The seam is deliberately SHIPPED AHEAD OF ITS
# CALLERS: the portal is read-only today (three mutation routes return 501), and
# the point of the issue is to settle the read-model requirements that light
# mutability imposes BEFORE the read model sets, rather than reopening it
# immediately afterwards. Each helper is directly tested. These go stale when
# the mutation routes become real.
github.com/goobers/goobers/internal/httpapi.requireSourceApplied # If-Source-Applied enforcement, wired when reads take the header (#1934).
github.com/goobers/goobers/internal/httpapi.requireIfMatch # Concurrent-edit detection for definition edits (#1934).
github.com/goobers/goobers/internal/httpapi.normaliseETag # ETag comparison on the digest rather than the formatting (#1934).
github.com/goobers/goobers/internal/httpapi.definitionETag # Renders a definition digest as an ETag (#1934).
github.com/goobers/goobers/internal/httpapi.admissionController.inFlight # Per-class occupancy for diagnostics (#1926); its tests are _test.go, which this gate does not count.

# FakeAdapter is the shared harness test double, promoted to a non-test file so
# tests in other packages (cmd/goobers) can import it (#2886). Only *_test.go
# reference it and its helpers, which this gate does not count.
github.com/goobers/goobers/internal/harness.FakeAdapter.Name # shared harness test double (#2886); only _test.go callers.
github.com/goobers/goobers/internal/harness.FakeAdapter.Preflight # shared harness test double (#2886); only _test.go callers.
github.com/goobers/goobers/internal/harness.FakeAdapter.Run # shared harness test double (#2886); only _test.go callers.
github.com/goobers/goobers/internal/harness.FakeAdapter.RunNested # shared harness test double (#2886, nested-agent support #3547); only _test.go callers.
github.com/goobers/goobers/internal/harness.FakeAdapter.ValidateNestedAgentPolicy # shared harness test double (#2886, nested-agent support #3547); only _test.go callers.
github.com/goobers/goobers/internal/harness.FakeAdapter.run # shared harness test double's private Run/RunNested helper (#2886, nested-agent support #3547); only _test.go callers.
github.com/goobers/goobers/internal/harness.WriteCompletion # shared harness completion-file test helper (#2886); only _test.go callers.

# testdep is a cross-package test-dependency guard (skips a test when a required
# CLI/env is absent); it must be an importable package rather than a _test.go
# helper so other packages' tests can call it (#2886). Only *_test.go reference it.
github.com/goobers/goobers/internal/testdep.Dependencies # cross-package test-dep guard (#2886); only _test.go callers.
github.com/goobers/goobers/internal/testdep.Require # cross-package test-dep guard (#2886); only _test.go callers.
github.com/goobers/goobers/internal/testdep.RequireEnv # cross-package test-dep guard (#2886); only _test.go callers.
github.com/goobers/goobers/internal/testdep.require # cross-package test-dep guard (#2886); only _test.go callers.

github.com/goobers/goobers/internal/temporaltest.StartDevServer # Dev-server acquisition seam used only from engine/bootstrap _test.go files (#3393).
github.com/goobers/goobers/internal/temporaltest.resolveDevServerAcquisition # Offline-vs-download resolution helper for the test-only dev-server seam (#3393).
github.com/goobers/goobers/internal/livejournal.WithScrubber # Live-journal writer option exercised by tests; production path uses the default scrubber until #3513 wires per-run scrubbers.
github.com/goobers/goobers/internal/livejournal.WithClock # Live-journal writer clock option exercised by tests (idle-close determinism).

# Goobernetes dispatcher (#3513): the engine-side substrate is production-wired by the
# #3588 mode-3 cutover (goobers worker --dispatch-namespace binds the dispatch queues and
# serves DispatchStage). What remains exempt is the POD-side surface — the stage-credential
# resolve and the egress-probe acceptance tooling — whose callers are the in-pod stage
# runtime (image contract, docs/design/goobernetes-deployment-images.md) and the v1 exit
# smoke (#3517).
#
# BlobClient is no longer on that list. #3823 wired the pod's context-materialization path
# (cmd/goobers/dispatchcontext.go), which calls BlobClient.Describe directly for the
# blob-plane diagnostic and hands the client to workerhost.MaterializeContext as a
# blobstore.Store — an interface that declares Has. Both methods are reachable from
# production now, so exempting them would be a stale entry and test/deadcode rejects it.
#
# #3805 removed the same two entries independently, on a WEAKER footing — the pod span
# recorder then carried a *dispatcher.BlobClient field, which only put the type in the
# analyzer's runtime-type set. That field is gone (spans now ride #3823's one
# write-through), and this reachability does not depend on it: dispatchcontext.go's
# Describe call is a direct, named production caller.
github.com/goobers/goobers/internal/dispatcher.EgressTriple.Verdict # Pod-side surface, test-exercised; the caller is the in-pod stage runtime / #3517 smoke, not the engine (#3588 wired the engine side).
github.com/goobers/goobers/internal/dispatcher.ProbeOutcome.String # Pod-side surface, test-exercised; the caller is the in-pod stage runtime / #3517 smoke, not the engine (#3588 wired the engine side).
github.com/goobers/goobers/internal/dispatcher.ProbeTCP # Pod-side surface, test-exercised; the caller is the in-pod stage runtime / #3517 smoke, not the engine (#3588 wired the engine side).
github.com/goobers/goobers/internal/dispatcher.ResolveStageCredentials # Pod-side surface, test-exercised; the caller is the in-pod stage runtime / #3517 smoke, not the engine (#3588 wired the engine side).
github.com/goobers/goobers/internal/runnercap.ParseRunnerClassPreimage # #3568 decodes the runner-class-restrictions annotation back to a restriction set (inverse of RunnerClassPreimage); exercised by the renderer round-trip tests, no in-binary caller yet — a diagnostic decode helper for operators/preflight.

# internal/e2e (#3517, graduated from test/goobernetes): `goobers e2e verify`
# (cmd/goobers/e2everify.go) wires the assertion helpers checkable from one
# run's RECORDED data alone or from --expected topology input — S1, S2, S8
# (journal proxy), ARCH11-7, ARCH11-8, S9. The symbols below need a live
# topology-driving orchestration this command deliberately does not do
# (S3-S7's declared-edge handoff/artifact-materialization/repass/write-API
# evidence, and S6's six-cell kill matrix, all TOPOLOGY-PENDING per
# internal/e2e/doc.go and each file's own doc comment) — the consumer-side
# orchestration named in #3517 is their eventual caller. All are unit-tested
# against fixtures today.
github.com/goobers/goobers/internal/e2e.AssertArtifactMaterialization # S4, topology-pending: needs a live cross-node materialize-before-start record no run journal carries yet.
github.com/goobers/goobers/internal/e2e.AssertDeclaredEdgeHandoff # S3, topology-pending: needs the runtime-half continuity record (#3505) a live driver reads at the two repo boundaries.
github.com/goobers/goobers/internal/e2e.AssertRepassFreshNodeWithVerdict # S5, topology-pending: needs the repass attempt's recorded input pointers, runner-internal state no read-model field exposes today.
github.com/goobers/goobers/internal/e2e.AssertTriggerAndEscalationViaWriteAPI # S7, topology-pending: needs the live procedure's own operator transcript, which only a live smoke run produces.
github.com/goobers/goobers/internal/e2e.GateVerdictPointerName # S5 helper, called only by the topology-pending AssertRepassFreshNodeWithVerdict above.
github.com/goobers/goobers/internal/e2e.AllStageClasses # S6 kill-matrix axis enumeration; topology-pending (no live CellDriver implementation exists — internal/e2e/killmatrix.go's own doc comment).
github.com/goobers/goobers/internal/e2e.AllFailureKinds # S6 kill-matrix axis enumeration; topology-pending, see AllStageClasses above.
github.com/goobers/goobers/internal/e2e.KillMatrix # S6 kill-matrix cell enumeration; topology-pending, see AllStageClasses above.
github.com/goobers/goobers/internal/e2e.ClassifyCellResult # S6 kill-matrix per-cell classifier; topology-pending, see AllStageClasses above.
github.com/goobers/goobers/internal/e2e.RunKillMatrix # S6 kill-matrix driver loop; topology-pending, see AllStageClasses above.
github.com/goobers/goobers/internal/e2e.NewStaticCellDriver # S6 kill-matrix fixture double for RunKillMatrix's own tests; topology-pending, see AllStageClasses above.
github.com/goobers/goobers/internal/e2e.StaticCellDriver.FailAt # S6 kill-matrix fixture double method; topology-pending, see AllStageClasses above.
github.com/goobers/goobers/internal/e2e.StaticCellDriver.Inject # S6 kill-matrix fixture double method; topology-pending, see AllStageClasses above.
github.com/goobers/goobers/internal/e2e.Violation.String # §5 rule 1 "no kubectl exec" guard formatter; not wired into any lint/CI entrypoint yet, only its own tests.
github.com/goobers/goobers/internal/e2e.matchLine # §5 rule 1 guard's shared pattern match; unreachable once its two exported callers (below) are, only its own tests exercise it.
github.com/goobers/goobers/internal/e2e.ScanTextForKubectlExec # S7's transcript half of the §5 rule 1 guard; topology-pending with AssertTriggerAndEscalationViaWriteAPI above (its only intended caller).
github.com/goobers/goobers/internal/e2e.ScanGoStringLiteralsForKubectlExec # §5 rule 1 guard over this package's own Go source; not wired into any lint/CI entrypoint yet, only its own tests.
github.com/goobers/goobers/internal/e2e.ReadBundle # Bundle.Encode's round-trip counterpart; no consumer reads a written evidence bundle back yet (a future re-run/comparison feature), only its own tests.

# Fleet connector (#3921) vertical slice: this PR ships discovery, enrollment,
# and storage now; the WSS hello/heartbeat/reconnect connection loop and its
# CLI/daemon callers land in a follow-up increment of this same PR series.
github.com/goobers/goobers/internal/fleet.Client.Join # Convenience wrapper around Discover+JoinDiscovered for a future non-CLI caller (the CLI join path calls JoinDiscovered directly today); exercised by client_test.go.
github.com/goobers/goobers/internal/fleet.ParsePrivateKey # Round-trip counterpart to MarshalPrivateKey, needed once the WSS connection loop reloads a stored private key to sign challenges; not yet called or tested.
github.com/goobers/goobers/internal/fleet.errorsNewInvalidKey # ParsePrivateKey's own error helper; unreachable once ParsePrivateKey is, see above.
github.com/goobers/goobers/internal/fleet.ChallengePayload # Shared payload builder for the WSS hello/challenge handshake, whose connection loop is planned but not yet implemented; exercised by signature_test.go.
github.com/goobers/goobers/internal/fleet.SignChallenge # Instance side of the WSS hello/challenge handshake above; exercised by signature_test.go.
github.com/goobers/goobers/internal/fleet.VerifyChallenge # Fleet-service side of the WSS hello/challenge handshake above, retained here so the protocol keys and payload stay symmetric; exercised by signature_test.go.
